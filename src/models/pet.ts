/**
 * Swagger PetstoreLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  bigint,
  lazy,
  object,
  optional,
  Schema,
  string,
} from '../schema.js';
import { Category2, category2Schema } from './category2.js';
import { StatusEnum, statusEnumSchema } from './statusEnum.js';
import { Tag, tagSchema } from './tag.js';

export interface Pet {
  id?: bigint;
  category?: Category2;
  name: string;
  photoUrls: string[];
  tags?: Tag[];
  status?: StatusEnum;
}

export const petSchema: Schema<Pet> = object({
  id: ['id', optional(bigint())],
  category: ['category', optional(lazy(() => category2Schema))],
  name: ['name', string()],
  photoUrls: ['photoUrls', array(string(), { xmlItemName: 'photoUrl' })],
  tags: [
    'tags',
    optional(array(lazy(() => tagSchema), { xmlItemName: 'Tag' })),
  ],
  status: ['status', optional(statusEnumSchema)],
});
